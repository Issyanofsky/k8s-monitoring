apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-last.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-last.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-last.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
